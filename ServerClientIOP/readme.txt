codigo fuente realizado con qt6creator 
compilador qt 5.15.2 mingw 64bits
recordar que hay que usar algunas librerias de qt siempre de este compilador

cuando se compila desde qt la carpeta output es creada con los binarios en sus 
respectivas carpetas, para la instalacion, ejecutar installWindow.bat con la
opcion d o r (debug o release) segun se haya compilado en qt.

Posteriormente, lanzar el ejecutable